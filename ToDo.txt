
1) convert amplitudes of partials to loudnesses??

2) create gui for the user to change the spectrum

3) implement adaptive tuning? => dynamic
or at least automatic (ideal) spectrum generation for a given scale (e.g. 11-tet) => static

4) plot/draw dissonance curve for the current spectrum and mark the steps of the current scale



--------GIT COMMANDS:-------
git init
git status
git add .
git commit -m "message"
git push -u "url" <branch>
git checkout <branch>